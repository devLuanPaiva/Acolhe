// Define the type for caregiver
export interface Caregiver {
  id: number;
  name: string;
  gender: string; // Male, Female, etc.
  age: number;
  city: string;
  description: string;
  image: string;
}
